import styled from 'styled-components';
import { tertiaryColor } from '../../utils/styleVariables';

const SecoundaryHeadline = styled.h2`
    font-size: 2.3rem;
    font-weight: 500;
    color: ${tertiaryColor};
    text-align: center;
    margin: 2rem 0;
`;

export default SecoundaryHeadline;
